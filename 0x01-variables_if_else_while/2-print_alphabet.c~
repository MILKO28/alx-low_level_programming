#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 1 (Success)
 */
int main(void)
{
    int alpha = 'a';
    while (alpha < 'z')
{
putchar (alpha);
alpha++;
}
putchar (alpha,"\n");
return (0);
}
