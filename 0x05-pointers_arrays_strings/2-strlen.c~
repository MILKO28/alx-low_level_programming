#include "main.h"

/**
 * _strlen - a function that returns the length of a string.
 * @s: pointer 
 */
int _strlen(char *s)
{
	int i;
        char str = *s;
	
	for (i = 0; i != '\0'; i++)
	{
		str[i];
	}
	return i+1;
}
