#include "main.h"

/**
 * _strlen - a function that returns the length of a string.
 * @s: pointer
 * Return: number of strings
 */
int _strlen(char *s)
{
	int i = 0;

	for (; s[i] != '\0'; i++)
	{
		j++;
       	}
	return (j);
}
